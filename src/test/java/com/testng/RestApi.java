package com.testng;

import org.openqa.selenium.remote.Response;
import org.testng.Assert;

import io.restassured.RestAssured;

public class RestApi<RequestSpecification> {

	/*
	 * @Test private void m1() {
	 * 
	 * 
	 * 
	 * RestAssured.baseURI="https://reqres.in/"; RequestSpecification
	 * req=RestAssured.given(); Response response=req.get("api/users/2"); String
	 * asString=response.asString(); System.out.println(asString); int
	 * statuscode=response.getSTatusCode(); System.out.println(statuscode);
	 * Assert.assertEquals(201, statuscode);
	 * 
	 * 
	 * }
	 */
}
